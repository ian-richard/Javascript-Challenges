// a function which takes an array and returns true if three numbers in the array total 7.

const total7 = (arr, sum) => {
    let result = false;

    // for (let i = 0; i < arr.length - 2; i++){
    //     for (let j = i+1; j < arr.length - 1; j++){
    //         for (let k = j + 1; k < arr.length; j++){
    //             if (arr[i] + )
    //         }
    //     }
    // }
}

